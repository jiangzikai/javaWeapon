---------------------字符串String-------------------------------------------
字符串: 字符序列 多个字符 "" -->引用类型,是个类不是基本数据类型
字符: '' 单个字符 -->基本类型
1、+: 拼接，不是追加 ,会产生大量的垃圾
2、charAt(索引) -->索引|位置 [0,length())
3、length():长度
4、equals()比较内容 -->String将equals()方法重写了，从比较地址变为比较内容
equalsIgnoreCase()忽略大小写比较内容


每个用双引号括起来的字符串都是String类的一个实例：
String e="";//ann empty string
String greeting = "Hello";


----------------------------------------------------------------
String
不能修改java字符串中的字符，所以java文档中将String类对象称为不可变字符串
